# Railway Environment Variables for finance-tracker deployment
# Copy these to your Railway project's environment variables

# Database URL - Railway provides this automatically when you provision a PostgreSQL database
# Format: postgresql://user:password@host:port/database
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Secret - REQUIRED - Generate a secure random string
# Example: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-here-change-this

# Port - Railway sets this automatically, no need to configure
# PORT=${{PORT}}

# Node Environment
NODE_ENV=production
